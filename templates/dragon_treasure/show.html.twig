{% extends 'base.html.twig' %}

{% block title %}DragonTreasure{% endblock %}

{% block body %}
    <h1>DragonTreasure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dragon_treasure.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dragon_treasure.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dragon_treasure.description }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ dragon_treasure.value }}</td>
            </tr>
            <tr>
                <th>CoolFactor</th>
                <td>{{ dragon_treasure.coolFactor }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ dragon_treasure.createdAt ? dragon_treasure.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IsPublished</th>
                <td>{{ dragon_treasure.isPublished ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dragon_treasure_index') }}">back to list</a>

    <a href="{{ path('app_dragon_treasure_edit', {'id': dragon_treasure.id}) }}">edit</a>

    {{ include('dragon_treasure/_delete_form.html.twig') }}
{% endblock %}
