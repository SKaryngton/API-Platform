<form method="post" action="{{ path('app_dragon_treasure_delete', {'id': dragon_treasure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dragon_treasure.id) }}">
    <button class="btn">Delete</button>
</form>
